# Manuscript

Build with:

```bash
cd ..
make paper
```

The boxed status note is intentionally present until licensed WRDS stock-level estimates replace the pre-specified exhibit map. Synthetic figures belong only in the appendix.
