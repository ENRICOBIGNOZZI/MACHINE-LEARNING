# Publish to GitHub

Intended repository:

```text
ENRICOBIGNOZZI/certified-stock-ranking
```

After creating the empty repository on GitHub or authenticating GitHub CLI:

```bash
git remote add origin https://github.com/ENRICOBIGNOZZI/certified-stock-ranking.git
git push -u origin main
```

The archive already contains a committed `main` branch and GitHub Actions tests.
